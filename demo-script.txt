# DEMO SCRIPT FOR AGENTIC PLANNER CLI
# Execute these commands during asciinema recording
# Total time: ~30 seconds with 2-3 second pauses between commands

docker run --rm agentic-planner-cli --help

docker run --rm agentic-planner-cli "explain quantum computing to me"

docker run --rm agentic-planner-cli "teach me about machine learning" --explain

docker run --rm agentic-planner-cli "what should I know about blockchain" --model-profile lite

# Recording commands:
# asciinema rec demo.cast
# [execute commands above with 2-3 second pauses]
# exit (or Ctrl+D)
# asciinema play demo.cast (to preview)
# agg demo.cast demo.gif (to convert to GIF)